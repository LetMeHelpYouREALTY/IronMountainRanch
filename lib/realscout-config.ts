/**
 * RealScout widget IDs and in-site conversion URLs (Iron Mountain Ranch).
 * Agent ID is shared across portfolio; data stays per-site via page copy + UTM.
 */

export const REALSCOUT_AGENT_ENCODED_ID = "QWdlbnQtMjI1MDUw";

/** In-site MLS search — prefer over external portal exits */
export const REALSCOUT_LISTINGS_PATH = "/listings";
export const REALSCOUT_BUY_PATH = "/buy";
export const REALSCOUT_HOME_VALUE_PATH = "/home-valuation";
